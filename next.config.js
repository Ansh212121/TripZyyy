/** @type {import('next').NextConfig} */
const nextConfig = {
  // You can add other Next.js config options here
  // e.g., images, redirects, rewrites, etc.
  
  // Note: experimental.serverActions has been removed, as Server Actions
  // are enabled by default in Next.js 14.
};

module.exports = nextConfig;
